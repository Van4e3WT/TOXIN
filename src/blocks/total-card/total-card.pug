include ~Blocks/twin-datepick/twin-datepick.pug
include ~Blocks/dropdown-absolute/dropdown-absolute.pug
include ~Blocks/tooltip/tooltip.pug
include ~Blocks/button/button.pug

mixin total-card(props)
  -
    const {
      number,
      isLux,
      cost,
      days = 1,
      daysForm = 'сутки',
      twinDatepick,
      dropdownAbsolute,
      positions = [],
      button,
    } = props;

    const toSpacedString = (number) => {
      const arrNumber = String(number).split('');

      arrNumber.splice(-3, 0, '\xa0');

      return arrNumber.join('');
    }

    let totalSum = cost * days;

  form.total-card
    .total-card__header
      .total-card__room-number
        h3.total-card__prefix №
        h1.total-card__number=number
        if isLux
          h3.room-number__lux люкс
      .total-card__room-cost
        h3.total-card__rubles=`${cost}₽`
        .total-card__period в сутки
    .total-card__body
      .total-card__input
        -
          const {
            defaultValue = 'date dropdown',
            field1 = defaultValue,
            field2 = defaultValue,
          } = twinDatepick;

        +twin-datepick(field1, field2)
      label.total-card__label Гости
      .total-card__input
        +dropdown-absolute(dropdownAbsolute)
      .total-card__calculate
        .total-card__product
          .total-card__position
            span.total-card__cost=`${toSpacedString(cost)}₽`
            span.total-card__split &nbsp;x&nbsp;
            span.total-card__peroid=`${days} ${daysForm}`
          .total-card__period-cost=`${toSpacedString(totalSum)}₽`
        each position in positions
          -
            const { content, tooltip, cost: positionCost = 0, discont = 0 } = position;
            totalSum = totalSum + positionCost - discont;

          .total-card__product
            .total-card__position.total-card__position_service
              span.total-card__cost=content
              +tooltip(tooltip)
            .total-card__period-cost=`${positionCost}₽`
    .total-card__total
      h2.total-card__result Итого
      .total-card__space
      h2.total-card__cost=`${toSpacedString(totalSum)}₽`
    +button(button)