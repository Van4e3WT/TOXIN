mixin button(props)
  -
    const { reference="/plug-link", content="Button", modificators } = props;
    const modificatorStr = modificators ? ` ${modificators.map((item) =>
      `button_${item}`).join(' ')}` : '';
  a.button(class=modificatorStr, href=reference)
    .button__content=content